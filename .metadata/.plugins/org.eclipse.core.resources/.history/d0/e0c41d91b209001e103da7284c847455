package myProejct;

public class Main4 {
	public static void main(String[] args) {
		Main4 main = new Main4();
		main.solution(1, 6);
	}
	private int solution(int a, int b) {
		Dice dice1 = new Dice(a);
		Dice dice2 = new Dice(b);
		
		if(!dice1.isEvenNumber()&&!dice2.isEvenNumber()) {
			return (int)(Math.pow(dice1.getScore(),2) +Math.pow(dice2.getScore(), 2));
		}
		System.out.println(Math.pow(2.0, 3.0));
		return 0;
	}
}
class Dice{
	private int score;
	private boolean isEvenNumber;
	public Dice(int score) {
		try {
			if(score<1 || score>6) {
				throw new Exception("êîéöÇ™ê≥ÇµÇ≠Ç†ÇËÇ‹ÇπÇÒÅB");
			}
			this.score=score;
			this.isEvenNumber = score%2==0? true : false;
		} catch (Exception e) {
			e.printStackTrace();
		}
	}
	public int getScore() {
		return score;
	}

	public void setScore(int score) {
		this.score = score;
	}
	public boolean isEvenNumber() {
		return isEvenNumber;
	}
	public void setEvenNumber(boolean isEvenNumber) {
		this.isEvenNumber = isEvenNumber;
	}
	
	
}